# 注意：`.env.example` 中 `VITE_API_BASE` 为可选。
#
# 本地开发：
#   1. `cd server && mvn spring-boot:run`（后端 8080，自动初始化数据库）
#   2. `cd pc && npm install && npm run dev`（前端 5174，vite 代理 /api -> 8080）
#
# 生产部署（GitHub Pages）：
#   1. 在仓库 Settings -> Secrets and variables -> Actions -> Variables 配置 `VITE_API_BASE`
#      （指向你部署的后端地址，如 https://api.example.com）
#   2. 推送到 main 即自动构建发布 pc/dist -> Pages
#
# 若未配置 VITE_API_BASE，前端将回退到同域 /api（与后端同域部署可用）。
# 仅用于后端程序内校验逻辑，不含密钥。